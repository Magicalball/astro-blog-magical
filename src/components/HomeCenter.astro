<div class="logodiv">
  <p class="logotext">Magical_ball's Blog</p>
</div>

<style>
  .logodiv {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    /* 防止字体加载时的布局抖动 */
    min-height: 3.2rem;
  }
  .logotext {
    font-family: "Caveat", serif;
    font-optical-sizing: auto;
    font-weight: 400;
    font-style: normal;
    font-size: 2.5rem;
    color: #000;
    transition: transform 0.3s ease;
    /* 锁定文本容器尺寸防止抖动 */
    width: 100%;
    text-align: center;
    white-space: nowrap;
    /* 为Caveat字体预留合适空间 */
    min-width: 320px;
    line-height: 1.2;
    cursor: default; /* 避免出现文本输入光标，保持默认箭头光标 */
    user-select: none;
  }

  :global(.dark) .logotext {
    color: #ffffff;
  }
  .logodiv:hover .logotext {
    transform: scale(1.05);
  }

  /* 移动端响应式适配 */
  @media (max-width: 636px) {
    .logotext {
      min-width: 280px;
      font-size: 2.2rem;
    }
    .logodiv {
      min-height: 2.8rem;
    }
  }

  @media (max-width: 480px) {
    .logotext {
      min-width: 240px;
      font-size: 1.8rem;
    }
    .logodiv {
      min-height: 2.4rem;
    }
  }
</style>
