<div class="homeabout">
  <div class="background-container"></div>
  <a href="/about" class="hasecond"><p>点这里了解我</p></a>
</div>

<style>
  .homeabout {
    width: 100%;
    height: 100%;
    position: relative;
    border-radius: 12px;
  }

  .background-container {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-color: #f5f5f5;
    background-image: url('/images/homea.webp');
    background-size: cover;
    background-position: right;
    z-index: 1;
    transition: opacity 0.3s ease;
    border-radius: 12px;
  }

  :global(.dark) .background-container {
    background-color: #2a2a2a;
  }

  .hasecond {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;
    height: 50%;
    text-align: center;
    align-content: center;
    font-size: 2rem;
    color: #fff;
    text-decoration: none;
    background-color: rgba(141, 141, 141, 0.5);
    padding: 0.5rem 1rem;
    z-index: 2;
    transition: all 0.3s ease;
    opacity: 0;
  }

  :global(.dark) .hasecond {
    background-color: rgba(60, 60, 60, 0.7);
  }

  .homeabout:hover .background-container {
    opacity: 0.75;
    filter: blur(4px);
  }

  .homeabout:hover .hasecond {
    opacity: 1;
  }

  p {
    position: relative;
    display: inline-block;
  }

  p::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    height: 2px;
    background-color: #fff; /* 下划线 */
    transform: scaleX(0);
    transform-origin: right;
    transition: transform 0.3s;
  }

  p:hover::after {
    transform: scaleX(1);
    transform-origin: left;
  }
</style>
